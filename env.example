OPENROUTER_API_KEY=
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# ─────────────────────────────────────────────────────────────────────────────
# Model menu + server-side models cache
# ─────────────────────────────────────────────────────────────────────────────
#
# The app fetches OpenRouter's Models API dynamically and caches results server-side.
# Defaults are resolved by matching these names against model display names (not IDs).
#
MODEL_CACHE_TTL_SECONDS=3600
DEFAULT_ANALYSIS_MODEL_NAME=GPT 5.2
DEFAULT_GENERATION_MODEL_NAME=GPT 5 Image

# Legacy overrides (model IDs). Prefer using the name-based defaults above.
# OPENAI_MODEL=openai/gpt-5.2
# OPENROUTER_IMAGE_MODEL=black-forest-labs/flux.2-pro

# Optional fallback name for the same key
OPENAI_API_KEY=

PORT=5174

# ─────────────────────────────────────────────────────────────────────────────
# RAG (Retrieval-Augmented Generation)
# ─────────────────────────────────────────────────────────────────────────────
#
# Optional: enable a local knowledge base index and use it to augment prompt generation.
# Build the local index with:
#   npm run rag:ingest
#
RAG_ENABLED=false
# RAG_INDEX_DIR=data/rag
# RAG_EMBEDDING_MODEL_ID=Xenova/e5-small-v2
# DEFAULT_RAG_DRAFT_MODEL_NAME=Grok 4 Fast
# DEFAULT_RAG_FINAL_MODEL_NAME=GPT 5.2
# RAG_TOP_K=6
# RAG_MAX_CONTEXT_CHARS=8000

# ─────────────────────────────────────────────────────────────────────────────
# Map generation (depth/edges/masks)
# ─────────────────────────────────────────────────────────────────────────────
#
# The server spawns a Python worker at `server/scripts/generate_maps.py`.
# If you run `server/scripts/setup_maps_env.sh`, point MAPS_PYTHON_PATH at the venv:
#   MAPS_PYTHON_PATH=./server/scripts/.venv/bin/python3
#
MAPS_ENABLED=true
# MAPS_PYTHON_PATH=python3
# MAPS_PYTHON_PATH=./server/scripts/.venv/bin/python3
# MAPS_MAX_IMAGE_MP=2
# MAPS_CACHE_DIR=map_cache
# MAPS_DEPTH_ENABLED=true
# MAPS_NORMALS_ENABLED=true
# MAPS_EDGES_ENABLED=true
# MAPS_SEGMENTATION_ENABLED=false
# MAPS_FACE_MASK_ENABLED=true
# MAPS_HANDS_MASK_ENABLED=true
